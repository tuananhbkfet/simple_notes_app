// This is now an empty component since we're removing theme toggle functionality
import * as React from 'react';

export function ThemeToggle() {
  // Return null - no theme toggle is needed anymore
  return null;
}
